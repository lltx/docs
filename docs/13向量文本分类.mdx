---
title: 文本向量化分类
---


本文将展示如何通过LangChain4j框架结合OpenAI模型，构建一个情感分类器。我们将从概念入手，详细介绍如何在Java中实现这一功能。

## 什么是文本分类
文本分类是自然语言处理中的核心任务，它将文本分配到不同的类别中。应用广泛，如垃圾邮件过滤、新闻推荐、情感分析等。

### 应用场景
+ **垃圾邮件过滤**：检测和过滤垃圾邮件。
+ **新闻推荐**：根据用户兴趣推荐相关新闻。
+ **情感分析**：分析用户评论的情感倾向。
+ **仇恨言论识别**：过滤论坛中的违规发言。
+ **营销与广告**：根据用户反馈调整广告策略。

## LangChain4j中的文本分类支持
LangChain4j框架通过`TextClassifier`接口，结合嵌入模型，实现强大的文本分类功能。

### TextClassifier接口
```java
package com.pig4cloud.ai.classification;

import java.util.List;

public interface TextClassifier<E extends Enum<E>> {

    /**
     * 对文本进行分类
     *
     * @param text 需要分类的文本
     * @return 文本所属的分类列表
     */
    List<E> classify(String text);

    /**
     * 对文本片段进行分类
     *
     * @param textSegment 需要分类的文本片段
     * @return 文本所属的分类列表
     */
    default List<E> classify(TextSegment textSegment) {
        return classify(textSegment.text());
    }

    /**
     * 对文档进行分类
     *
     * @param document 要分类的文档
     * @return 文本所属的分类列表
     */
    default List<E> classify(Document document) {
        return classify(document.text());
    }
}
```

### 基于嵌入模型的分类器实现
`EmbeddingModelTextClassifier`通过嵌入模型对文本进行向量化，并利用余弦相似度计算文本与分类标签之间的匹配度。

```java
package com.pig4cloud.ai.classification;

import java.util.List;
import java.util.Map;

public class EmbeddingModelTextClassifier<E extends Enum<E>> implements TextClassifier<E> {

    private final EmbeddingModel embeddingModel;
    private final Map<E, List<Embedding>> exampleEmbeddingsByLabel;
    private final int maxResults;
    private final double minScore;
    private final double meanToMaxScoreRatio;

    @Override
    public List<E> classify(String text) {
        Embedding textEmbedding = embeddingModel.embed(text).content();
        List<LabelWithScore<E>> labelsWithScores = new ArrayList<>();

        exampleEmbeddingsByLabel.forEach((label, exampleEmbeddings) -> {
            double meanScore = 0;
            double maxScore = 0;
            for (Embedding exampleEmbedding : exampleEmbeddings) {
                double cosineSimilarity = CosineSimilarity.between(textEmbedding, exampleEmbedding);
                double score = RelevanceScore.fromCosineSimilarity(cosineSimilarity);
                meanScore += score;
                maxScore = Math.max(score, maxScore);
            }
            meanScore /= exampleEmbeddings.size();
            labelsWithScores.add(new LabelWithScore<>(label, aggregatedScore(meanScore, maxScore)));
        });

        return labelsWithScores.stream()
                .filter(it -> it.score >= minScore)
                .sorted((a, b) -> Double.compare(b.score, a.score))
                .limit(maxResults)
                .map(it -> it.label)
                .collect(Collectors.toList());
    }

    private double aggregatedScore(double meanScore, double maxScore) {
        return (meanToMaxScoreRatio * meanScore) + ((1 - meanToMaxScoreRatio) * maxScore);
    }
}
```

### 参数说明
+ `maxResults`：返回的分类结果数量。
+ `minScore`：过滤得分低于该值的结果。
+ `meanToMaxScoreRatio`：用于调整最终得分时平均分与最大分的比例。

## 实现情感分析
### 依赖配置
在项目的`pom.xml`中加入OpenAI相关依赖：

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>

    <artifactId>langchain4j-openai-spring-boot-starter</artifactId>

</dependency>

<dependency>
    <groupId>dev.langchain4j</groupId>

    <artifactId>langchain4j</artifactId>

</dependency>

```

### 配置文件 `application.yml`
配置OpenAI的嵌入模型：

```yaml
langchain4j:
  openai:
    embedding-model:
      api-key: "your-openai-api-key"
      model-name: "text-embedding-ada-002"
```

### 枚举类定义
```java
package com.pig4cloud.ai.classification.enums;

import lombok.AllArgsConstructor;
import lombok.Getter;

@Getter
@AllArgsConstructor
public enum Sentiment {
    POSITIVE, NEUTRAL, NEGATIVE
}
```

### 示例数据
```java
package com.pig4cloud.ai.classification.example;

import com.pig4cloud.ai.classification.enums.Sentiment;

import java.util.List;
import java.util.Map;

public class SentimentExamples {
    public static final Map<Sentiment, List<String>> examples = Map.of(
            Sentiment.POSITIVE, List.of("This is great!", "Wow, awesome!"),
            Sentiment.NEUTRAL, List.of("Well, it's fine", "It's ok"),
            Sentiment.NEGATIVE, List.of("It is pretty bad", "Worst experience ever!")
    );
}
```

### Service实现
```java
package com.pig4cloud.ai.classification.service;

import dev.langchain4j.classification.EmbeddingModelTextClassifier;
import dev.langchain4j.model.openai.OpenAiEmbeddingModel;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import com.pig4cloud.ai.classification.enums.Sentiment;
import com.pig4cloud.ai.classification.example.SentimentExamples;

import java.util.List;

@Service
@RequiredArgsConstructor
public class ClassifyService {

    private final OpenAiEmbeddingModel openAiEmbeddingModel;

    /**
     * 对文本进行情感分析
     *
     * @param text 文本
     * @return 分类列表
     */
    public List<Sentiment> classifySentiment(String text) {
        EmbeddingModelTextClassifier<Sentiment> classifier =
                new EmbeddingModelTextClassifier<>(openAiEmbeddingModel, SentimentExamples.examples);
        return classifier.classify(text);
    }
}
```

### Controller实现
```java
package com.pig4cloud.ai.classification.controller;

import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import com.pig4cloud.ai.classification.enums.Sentiment;
import com.pig4cloud.ai.classification.service.ClassifyService;

import java.util.List;

@RestController
@RequestMapping("/classify/")
@RequiredArgsConstructor
public class ClassificationController {

    private final ClassifyService classificationService;

    @GetMapping("/sentiment")
    public List<Sentiment> sentiment(String text) {
        return classificationService.classifySentiment(text);
    }
}
```

### 测试
通过HTTP请求测试情感分类功能：

```plain
GET /classify/sentiment?text=Awesome! HTTP/1.1
Host: localhost:8080
Content-Type: application/json
```

## 总结
本文介绍了如何使用LangChain4j框架结合OpenAI的嵌入模型来实现文本分类，特别是情感分析任务。通过提供样例代码，演示了如何使用OpenAI的嵌入模型进行情感分类，并展示了如何将此解决方案扩展到其他文本分类任务中。

